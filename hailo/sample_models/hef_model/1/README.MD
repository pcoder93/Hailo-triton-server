# Download model files before running

Depending on your model you can have following configurations:

- only hef model OR
- only a onnx model OR
- have split a hef and onnx model.

The model.py expects to have altleast one of these files along with it.

- model.hef
- model.onnx
- model_head.onnx
